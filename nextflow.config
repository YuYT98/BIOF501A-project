singularity {
    enabled = true
}

process {
    container = "library://yuyt98/biof501a/project:0.1.0"
}

params {
    rawdata = "${baseDir}/data/rawdata.csv"
    unseenData = "${baseDir}/data/unseenData.csv"
    output = "${baseDir}/output"
}